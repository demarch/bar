# ============================================
# CONFIGURAÇÕES DO DOCKER COMPOSE
# ============================================
# Copie este arquivo para .env e ajuste os valores

# ============================================
# POSTGRES
# ============================================
POSTGRES_DB=bar_system
POSTGRES_USER=admin
POSTGRES_PASSWORD=CHANGE_ME_senha_segura_2024

# ============================================
# REDIS
# ============================================
REDIS_PASSWORD=CHANGE_ME_redis_password

# ============================================
# BACKEND
# ============================================
NODE_ENV=production
PORT=3001

# Database (use as mesmas credenciais do Postgres acima)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# Redis
REDIS_URL=redis://redis:6379

# JWT Secrets (ALTERE ESTES VALORES!)
JWT_SECRET=CHANGE_ME_generate_random_secret_here_min_32_chars
JWT_REFRESH_SECRET=CHANGE_ME_generate_another_random_secret_min_32_chars
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:3000

# Estabelecimento
NOME_ESTABELECIMENTO=Meu Bar
PERCENTUAL_COMISSAO_PADRAO=40

# ============================================
# FRONTEND
# ============================================
VITE_API_URL=http://localhost:3001
VITE_WS_URL=ws://localhost:3001

# ============================================
# INSTRUÇÕES
# ============================================
# 1. Copie este arquivo: cp .env.example .env
# 2. Edite o .env e altere TODOS os valores marcados com CHANGE_ME
# 3. Para gerar secrets seguros, use:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 4. NUNCA commite o arquivo .env no git!
